[package]
name = "mc-rpc"
description = "Generated JSON RPC Bindings for Minecraf Server Management Protocol Client"
authors = ["VilleOlof"]
license = "MIT"
version = "0.1.0"
edition = "2024"
keywords = ["minecraft", "rpc", "json", "client"]
repository = "https://github.com/VilleOlof/mc-rpc"
exclude = ["schema_fetch"]

[dependencies]
pale = "0.1.0"
serde = { version = "1.0", features = ["derive"] }
tokio-stream = { version = "0.1.17", features = ["sync"] }
serde_json = "1.0"

[build-dependencies]
serde_json = "1.0"
anyhow = "1.0"

[workspace]
members = ["schema_fetch"]
